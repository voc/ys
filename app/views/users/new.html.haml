%h2 Register new user

= render "shared/error_messages", :target => @user

= form_for(@user, as: :user) do |f|
  = render partial: 'form', locals: {f: f}, object: @user

  #button
    = f.submit 'Register'
